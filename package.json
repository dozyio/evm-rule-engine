{
  "name": "evm-rule-engine",
  "version": "0.0.1",
  "description": "A rule engine for EVM accounts and contract",
  "author": "dozyio",
  "license": "MIT",
  "scripts": {
    "typechain": "typechain --target ethers-v6 --out-dir typechain-types 'out/**/*.json'",
    "build:contract": "forge build",
    "test": "npm run build:contract && mocha --require ts-node/register --timeout 10000 --bail 'test/*.test.ts'",
    "test:coverage": "npm run build:contract && nyc --reporter=html --reporter=text --reporter=lcov mocha --require ts-node/register --timeout 10000 --bail 'test/*.test.ts' && open coverage/index.html"
  },
  "dependencies": {
    "@types/node": "^22.10.2",
    "ethers": "^6.13.4"
  },
  "devDependencies": {
    "@typechain/ethers-v6": "^0.5.1",
    "@types/chai": "^5.0.1",
    "@types/mocha": "^10.0.10",
    "chai": "^5.1.2",
    "mocha": "^11.0.1",
    "nyc": "^17.1.0",
    "ts-node": "^10.9.2",
    "typechain": "^8.3.2"
  }
}
